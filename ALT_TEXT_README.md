# Альтернативный текст для изображений

## 🎯 Что реализовано

Добавлены поля для альтернативного текста (alt) у всех изображений в проекте для улучшения SEO и доступности.

### 📋 Модели с новыми полями:

1. **Post** (статьи блога)
   - Поле: `image_alt` - альтернативный текст для превью статьи
   - Метод: `get_image_alt()` - возвращает `image_alt` или заголовок статьи

2. **TeamMember** (участники команды)
   - Поле: `photo_alt` - альтернативный текст для фото
   - Метод: `get_photo_alt()` - возвращает `photo_alt` или "Фото {имя}, {должность}"

3. **Testimonial** (отзывы)
   - Поле: `photo_alt` - альтернативный текст для аватара
   - Метод: `get_photo_alt()` - возвращает `photo_alt` или "Аватар {имя автора}"

## 🚀 Как использовать

### В админке Django:

1. **Откройте любую модель** с изображениями (Статьи, Команда, Отзывы)
2. **Найдите секцию** с изображением
3. **Заполните поле** "Альтернативный текст изображения/фото/аватара"
4. **Сохраните** - alt текст будет использоваться в HTML

### В шаблонах:

```django
<!-- Для статей -->
<img src="{{ post.image.url }}" alt="{{ post.get_image_alt }}">

<!-- Для команды -->
<img src="{{ member.photo.url }}" alt="{{ member.get_photo_alt }}">

<!-- Для отзывов -->
<img src="{{ testimonial.photo.url }}" alt="{{ testimonial.get_photo_alt }}">
```

## 📊 Преимущества

### SEO:
- ✅ Улучшает индексацию изображений поисковыми системами
- ✅ Помогает поисковикам понимать содержание изображений
- ✅ Повышает релевантность страниц

### Доступность:
- ✅ Помогает пользователям с нарушениями зрения
- ✅ Обеспечивает работу скрин-ридеров
- ✅ Соответствует стандартам веб-доступности (WCAG)

### Fallback:
- ✅ Если alt текст не заполнен, используется заголовок/имя
- ✅ Гарантирует наличие alt атрибута у всех изображений

## 🔧 Технические детали

### Миграция:
- Создана миграция `0007_post_image_alt_teammember_photo_alt_and_more.py`
- Поля добавлены как `CharField(max_length=200, blank=True)`

### Админка:
- Поля добавлены в соответствующие fieldsets
- Добавлены описания для помощи пользователям
- Поля необязательные (blank=True)

### Шаблоны:
- Обновлены все шаблоны для использования новых методов
- Сохранена обратная совместимость

## ✅ Готово к использованию!

Система полностью настроена и готова к работе. Теперь все изображения на сайте будут иметь правильные alt атрибуты для SEO и доступности.
